%h1 Books

%table.table
  %thead
    %tr
      %th
        \#
      %th
        Author
      %th
        Title
      %th
        Description
      %th
        Status
  %tbody
    - @books.each do |book|
      %tr
        %td
          = book.id
        %td
          = book.author
        %td
          = book.title
        %td
          = truncate(book.description)
        %td
          = book.status